To use show-modal module, import it in your script like so:
// import {displyLogin} from './utils/show-modal.js';

// Then you can call displayLogin(true) to show the modal
// and displayLogin(false) to hide it.

//OR

//to use it in in your html file, you can add '.js-show' class to the button that will show the modal
//and '.js-close' class to the button that will close the modal
// Then include the script in your html file like so: 
<script src="../script/utils/show-modal.js" type="module"></script>